# Test
We have different entities in our system which can be created/changed/removed. We need a tool which allows users to subscribe to the notifications about changes in the system, such as new appointment creation, escalated cases, and so on. User needs to be able to configure conditions on which notification will be sent, the notification text and means of notifications (email/phone). Also user and administrator should be able to manage existing notifications.

  - To specify accurate requirements please make reasonable assumptions and describe it.
  - Specification should contain user scenarios, requirements, proposed solution, mockups of UI.